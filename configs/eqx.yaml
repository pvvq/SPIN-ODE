default:
  seed: 42
  
  # logging savepath
  base_dir: tb_logs
  name: spin
  version: default

  # model
  solver:
    rtol: 1.e-6
    atol: 1.e-7
    max_steps: 100000
    checkpoints: 10000

  # training
  epochs: 2000
  learning_rate: 1.e-2
  ckpt_keep: 10
  ckpt_interval: 1000